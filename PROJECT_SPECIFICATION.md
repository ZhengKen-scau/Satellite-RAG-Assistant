# Satellite RAG Assistant 项目规范

## 1. 核心原则

### 1.1 统一专业风格
- 所有用户查询**必须通过RAGChain类进行处理**，禁止直接调用大模型API
- 所有响应必须统一使用**遥感专业风格**，避免多风格切换导致的混乱
- 强制模型使用简体中文回答，并在提示词中明确禁止英文输出

### 1.2 严格上下文依赖
- 必须逐字引用上下文中的具体数据，数值必须完全一致
- 禁止添加、猜测或推断任何上下文中不存在的信息
- 当上下文为空时，必须返回"根据提供的资料，我无法找到相关答案"

## 2. 文件格式支持规范

### 2.1 文档格式
| 格式 | 支持级别 | 处理方式 |
|------|----------|----------|
| PDF | 完全支持 | PyPDF2文本提取 |
| TXT/MD | 完全支持 | 多编码自动检测 |
| DOCX | 完全支持 | python-docx解析 |
| DOC | 基础支持 | 文件信息提取 + 内容推断 |
| CSV | 完全支持 | pandas结构化分析 |
| JSON | 完全支持 | json库解析 + 结构描述 |

### 2.2 遥感影像格式
| 格式 | 支持级别 | 依赖库 | 提取信息 |
|------|----------|--------|----------|
| GeoTIFF | 完全支持 | rasterio | 尺寸、波段、坐标系、分辨率、统计信息 |
| ERDAS IMG | 完全支持 | rasterio | 尺寸、波段、数据类型、坐标系 |
| ENVI系列 | 完全支持 | rasterio | 尺寸、波段、元数据标签 |
| NetCDF | 完全支持 | netCDF4 | 维度、变量、全局属性 |
| HDF/HDF5 | 完全支持 | h5py | 数据集结构、形状、数据类型 |

### 2.3 矢量数据格式
| 格式 | 支持级别 | 依赖库 | 提取信息 |
|------|----------|--------|----------|
| Shapefile | 完全支持 | geopandas | 要素数量、几何类型、坐标系、属性字段 |
| GeoJSON | 完全支持 | geopandas | 同上 |
| KML/KMZ | 完全支持 | geopandas | 同上 |
| GPX | 完全支持 | geopandas | 同上 |

### 2.4 图像格式
| 格式 | 支持级别 | 依赖库 | 提取信息 |
|------|----------|--------|----------|
| JPG/PNG/BMP/GIF | 基础支持 | Pillow | 尺寸、模式、格式 |

## 3. 技术实现规范

### 3.1 错误处理
- 所有文件处理必须包含try-catch异常处理
- 即使专业库不可用，也要提供基础文件信息
- 错误信息应友好且具有指导性

### 3.2 性能优化
- 大文件处理采用采样策略（如GeoTIFF统计信息仅对<1M像素计算）
- 内存敏感操作限制数据量（如CSV只读前10行）
- 避免不必要的文件重复读取

### 3.3 依赖管理
- 所有专业库必须在requirements.txt中声明
- 导入时使用try-except确保兼容性
- 提供清晰的安装和使用说明

## 4. API接口规范

### 4.1 请求格式
```json
{
  "query": "用户问题",
  "chat_history": "[{\"role\": \"user\", \"content\": \"...\"}, {\"role\": \"assistant\", \"content\": \"...\"}]",
  "files": ["可选的上传文件"]
}
```

### 4.2 响应格式
```json
{
  "answer": "结构化专业回答",
  "sources": ["相关上下文来源"],
  "processing_time": 0.123
}
```

## 5. 测试要求

### 5.1 功能测试
- 每种支持的文件格式必须有对应的测试用例
- 多轮对话场景必须验证上下文连贯性
- 边界情况（大文件、损坏文件、空文件）必须测试

### 5.2 性能测试
- 文件处理时间应在合理范围内
- 内存使用不应随文件大小线性增长
- 并发请求处理能力验证

## 6. 维护指南

### 6.1 新增格式支持
1. 在document_processor.py中添加新的文件处理方法
2. 更新README.md和PROJECT_SPECIFICATION.md
3. 添加对应的测试用例
4. 更新requirements.txt（如需要新依赖）

### 6.2 依赖更新
- 定期检查依赖库的安全性和兼容性
- 重大版本更新需全面测试
- 保持向后兼容性

## 7. 质量保证

### 7.1 代码质量
- 遵循PEP 8编码规范
- 关键函数必须有类型注解
- 复杂逻辑必须有详细注释

### 7.2 文档质量
- 所有新增功能必须同步更新文档
- 用户文档必须清晰易懂
- 技术文档必须准确完整